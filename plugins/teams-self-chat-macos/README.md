# Teams Self Chat for macOS Apple Silicon

Claude Code plugin that sends text, Markdown, or HTML to the signed-in user's Microsoft Teams self-chat through Microsoft Graph.

The first send opens Microsoft sign-in in the browser. Phase 1A keeps the token in memory, so restarting Claude Code requires signing in again.
